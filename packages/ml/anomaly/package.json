{
  "name": "@finance/ml-anomaly",
  "version": "0.0.1",
  "private": true,
  "scripts": {
    "dev": "uv run uvicorn app.main:app --port 8001",
    "start": "uv run uvicorn app.main:app --port 8001"
  }
}
