[tool.poetry]
name = "finance-anomaly-detector"
version = "0.1.0"
description = "Transaction Anomaly Detection using Isolation Forest and Rule-Based filters"
authors = ["hetro1887-ops <hetro1887-ops@users.noreply.github.com>"]

[tool.poetry.dependencies]
python = "^3.11"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
